

02. Listener

- XML 의 뷰를 클릭했을 때 원하는 동작을 할 수 있게끔 코드를 작성해야 한다
  그래서 XML 의 뷰를 Activity 로 가져와야 한다 가져온 다음 가져온 뷰에서 이벤트(클릭, 사용자의 input) 발생
  했을 때 우리가 원하는 동작을 실행 시켜야 한다 그때 사용해야하 는 것이 익명함수 이다.

- 익명함수
- 이름이 없는 함수/클래스
- 이름을 만들어줄 필요가 없다
- 한번만 사용한다(이름을 따로 명시하지 않는다, 나중에 따로 부를일이 없다)